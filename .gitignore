# --- Исходники / оригиналы (не нужны на сервере) ---
/*.zip
/*.mp4
/Hero image.png
/design/

# --- Локальные инструменты ---
/.claude/

# --- Будущий backend ---
node_modules/
.env
.env.*
!.env.example

# --- Данные (заказы, контент — не в git) ---
/data/*.json

# --- Загруженные через админку фото (генерируются на сервере) ---
/public/images/uploads/

# --- Загруженные через админку видео-отзывы (генерируются на сервере) ---
/public/videos/uploads/

# --- ОС / редакторы ---
Thumbs.db
.DS_Store
*.log
